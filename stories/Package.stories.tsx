import React from "react";
import { Package } from "./Package";

export default {
  title: "Package",
};

export const DefaultComponent = () => {
  return (
    <div>
      <Package />
    </div>
  );
};
